machine:
  python:
    version: "3.4.1"

dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
    - pep8 --ignore E201,E202 --max-line-length=120 --exclude='migrations' .
